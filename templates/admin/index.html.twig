{% extends 'base.html.twig' %}

{% block title %}Hello Tom!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div>
        {% for project in projects %}
            <p>{{ project.name }}</p>
        {% endfor %}
    </div>

    <p>Add a new project</p>
    <form>
        <label for="name">Enter a name</label><input type="text" id="name">
        <label for="description">Enter a description</label><input type="text" id="description">
        <label for="link">Enter the web URL</label><input type="text" id="link">
        <label for="git">Enter the GitHub link</label><input type="text" id="git">
    </form>

    <div class="example-wrapper">
        <h1>Hello! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a href="{{ 'src/Controller/UserController.php'|file_link(0) }}">src/Controller/UserController.php</a></code></li>
            <li>Your template at <code><a href="{{ 'templates/user/index.html.twig'|file_link(0) }}">templates/user/index.html.twig</a></code></li>
        </ul>
    </div>
{% endblock %}